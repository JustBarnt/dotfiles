[alias]
  co = checkout
  br = branch
  cm = commit -m
  df = diff
  st = status
  lg = log

  # Branch Management
  rename = branch -m

  # Fix Mistakes
  unstage = reset HEAD --
  discard = checkout --
